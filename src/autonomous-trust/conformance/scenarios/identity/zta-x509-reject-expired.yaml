schema_version: "1"
kind: scenario
protocol: identity
name: zta-x509-reject-expired
byte_pinning: false
description: |
    ZTA X.509 admission — expired credential. The newcomer announces a cert
    (expired.der) issued by the mission CA but past its notAfter. The verifier
    checks expiry first for a distinct status (EXPIRED), so the welcoming
    committee drops the candidate — propose_emitted == 0.

    Cross-impl: currently SKIPPED on the C side (the C conformance adapter's
    ZTA wiring is a tracked follow-up); the Python adapter pins this scenario.
    A skip on one side is not an asymmetric failure (diff_results.py).

participants:
  - id: bg
    role: border_guard
  - id: newcomer
    role: new_node

fixtures:
  zta_policy:
    enabled: true
    require_at_admission: true
    verifier_type: x509
    ca_bundle_path: testdata/zta/ca-bundle.pem
  credentials:
    newcomer: testdata/zta/certs/expired.der

steps:
  - id: 1
    from: newcomer
    to: broadcast
    function: request_access
    description: |
        newcomer announces an expired (but CA-issued) cert. bg rejects at
        admission; no propose is emitted.

expected_state:
  bg:
    propose_emitted: 0
