## Python Implementation

This directory contains the python source for the core of AutonomousTrust.

This code was the original implementation but is now *far behind the native code in terms of features.* It is no longer maintained, but remains as an option to facilitate rapid prototyping and simulation.
